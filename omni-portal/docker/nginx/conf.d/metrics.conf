# Nginx metrics endpoint for Prometheus
server {
    listen 80;
    server_name _;
    
    location /nginx-metrics {
        stub_status on;
        access_log off;
        allow 172.0.0.0/8;  # Docker network
        allow 127.0.0.1;
        deny all;
    }
}